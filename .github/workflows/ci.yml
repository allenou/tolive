
name: Deploy
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        persist-credentials: false

    - name: Build
      run: npm install && npm run build  
    
    - name: Move and make
      run: |
        mv favicon.ico .nojekyll .vitepress/dist/
        cd .vitepress/dist/ && echo 'tolive.toyou.xyz' > CNAME

    - name: Deploy
      uses: JamesIves/github-pages-deploy-action@releases/v3
      with:
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        BRANCH: gh-pages
        FOLDER: .vitepress/dist/